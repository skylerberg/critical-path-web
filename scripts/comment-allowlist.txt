# Sentences check-comments.mjs is allowed to see in more than one file.
#
# Only for a fact a reader needs *at the site*, where there is no module to hang
# it on — two independent dnd zones, two independent test setups. Everything else
# wants one owner and a shortened copy at the other end; adding a line here
# instead is how the duplication the check exists to catch gets re-admitted.
#
# One sentence per line, matched exactly after whitespace collapsing.

Keyboard drags end with a consider event (trigger DRAG_STOPPED), not a finalize, so the dragging flag must reset here too.
vi.spyOn hands back the existing spy when a method is already mocked, so without this a later test inherits an earlier one's call count.
Extra `users` lets one mock answer both the board fetch and the project-scoped users fetch the project shell fires on load.
